---

- name: Create bridge interface
  community.general.nmcli:
    conn_name: "{{ bridge_master }}"
    type: bridge
    state: present

- name: Create bridge slave interface
  community.general.nmcli:
    conn_name: "{{ bridge_slave }}"
    type: bridge-slave
    master: "{{ bridge_master }}"
    state: present
