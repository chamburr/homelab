---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zipline-data
  namespace: utility
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zipline-postgresql
  namespace: utility
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
