---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: zipline-redirect
  namespace: utility
spec:
  redirectRegex:
    regex: ^https://([^/]+)/?$
    replacement: https://$${1}/dashboard
