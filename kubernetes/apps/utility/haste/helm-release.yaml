---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: haste
  namespace: utility
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 2.0.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    global:
      nameOverride: haste
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/skyra-project/haste-server
              tag: latest@sha256:d4f3f9089bfe056184833dbdb8a0e35ccf4925bfc7388655eaa519b03ead1bde
            args:
              - /bin/sh
              - -c
              - sed -i -e 's/10/6/' -e 's/phonetic/random/' -e 's/500/50/' -e 's/about.*//'
                src/backend/lib/config.js && sed -i 's/.\\/data/\\/config/'
                src/backend/document_stores/file.js && yarn run start
            securityContext:
              fsGroup: 1000
    service:
      main:
        ports:
          http:
            port: 8290
    ingress:
      main:
        enabled: true
        className: traefik
        hosts:
          - host: p.${DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  name: main
                  port: http
        tls:
          - hosts:
              - p.${DOMAIN}
    persistence:
      config:
        enabled: true
        existingClaim: haste-data
        globalMounts:
          - path: /config
