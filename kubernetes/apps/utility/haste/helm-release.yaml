---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: haste
  namespace: utility
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.3.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    global:
      nameOverride: haste
    image:
      repository: ghcr.io/skyra-project/haste-server
      tag: latest@sha256:257d8a22fac263c063356faa0aa1858badb4ab02284c6c1ec01f0da8f63d758e
    args:
      - /bin/sh
      - -c
      - sed -i -e 's/10/6/' -e 's/phonetic/random/' -e 's/500/50/' -e 's/about.*//'
        src/backend/lib/config.js && sed -i 's/.\\/data/\\/config/'
        src/backend/document_stores/file.js && yarn run start
    service:
      main:
        ports:
          http:
            port: 8290
    persistence:
      config:
        enabled: true
        existingClaim: haste-data
    ingress:
      main:
        enabled: true
        ingressClassName: traefik
        hosts:
          - host: p.${DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - p.${DOMAIN}
    podSecurityContext:
      fsGroup: 1000
