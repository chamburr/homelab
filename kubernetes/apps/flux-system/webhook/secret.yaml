---
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: webhook-secret
  namespace: flux-system
spec:
  keys:
    - secret
  templates:
    token: "{% .Secrets.secret %}"
  path: secret/flux-system/webhook
  type: Opaque
