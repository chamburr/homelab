---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Receiver
metadata:
  name: webhook-receiver
  namespace: flux-system
spec:
  type: github
  events:
    - ping
    - push
  secretRef:
    name: webhook-secret
  resources:
    - kind: GitRepository
      name: flux-cluster
      namespace: flux-system
    - kind: Kustomization
      name: charts
      namespace: flux-system
    - kind: Kustomization
      name: crds
      namespace: flux-system
    - kind: Kustomization
      name: config
      namespace: flux-system
    - kind: Kustomization
      name: core
      namespace: flux-system
    - kind: Kustomization
      name: apps
      namespace: flux-system
