apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding
  namespace: utility
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
