controllers:
  main:
    containers:
      main:
        image:
          repository: docker.io/freshrss/freshrss
          tag: 1.29.1
        env:
          CRON_MIN: 0,30
service:
  main:
    controller: main
    primary: true
    ports:
      http:
        port: 80
ingress:
  main:
    className: traefik
    annotations:
      traefik.ingress.kubernetes.io/router.middlewares: >-
        networking-global-internal-only@kubernetescrd
    hosts:
      - host: rss.chamburr.xyz
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: main
              port: http
persistence:
  data:
    existingClaim: freshrss
    globalMounts:
      - path: /var/www/FreshRSS/data
