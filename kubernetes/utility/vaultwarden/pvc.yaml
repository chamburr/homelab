apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden
  namespace: utility
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
