---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: config
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
