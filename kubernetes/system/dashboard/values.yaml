nameOverride: dashboard
fullnameOverride: dashboard
app:
  ingress:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.middlewares: >-
        networking-global-internal-only@kubernetescrd
    ingressClassName: traefik
    hosts:
      - kubernetes.chamburr.xyz
    tls:
      enabled: false
kong:
  proxy:
    http:
      enabled: true
