apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: woodpecker
  namespace: developer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
