apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ntfy
  namespace: automation
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
