apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netmaker
  namespace: networking
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
