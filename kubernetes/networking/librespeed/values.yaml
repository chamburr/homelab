controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/librespeed/speedtest
          tag: 6.0.0
        env:
          MODE: standalone
service:
  main:
    controller: main
    primary: true
    ports:
      http:
        port: 8080
ingress:
  main:
    className: traefik
    hosts:
      - host: speedtest.chamburr.xyz
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: main
              port: http
