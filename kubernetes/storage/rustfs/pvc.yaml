apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
