apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wg-easy
  namespace: security
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
