apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-home
  namespace: security
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-block
